import React from 'react';
import Audit from './Audit';

const NodeConsole = () => {
  // Sharing component logic with Audit but mapping to a different intent
  return <Audit />;
};

export default NodeConsole;
